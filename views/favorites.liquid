{% render 'partials/head.liquid' %}

{% include 'partials/header.liquid' %}

<section class="art-wall">
    <h1 class="fav-title">Jouw favoriete kunstwerken</h1>
    <div class="fav-container">
        {% if art.size > 0 %}
            {% include 'partials/art-list.liquid' %}
        {% else %}
            <div class="empty-state">
            <p>😢 Je hebt nog geen favorieten toegevoegd.</p>
            <a href="/artwork" class="button">Bekijk kunstwerken</a>
            </div>
        {% endif %}
    </div>
</section>   

{% render 'partials/foot.liquid' %}

